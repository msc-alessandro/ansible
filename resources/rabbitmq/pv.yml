apiVersion: v1
kind: PersistentVolume
metadata:
  name: rabbitmq-data
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 12Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/pv_data"
